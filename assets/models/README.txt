把自訂的 3D 模型（.glb 檔）放進這個資料夾。

啟用方式：
1. 把模型命名為例如 report.glb、merit.glb、innovation.glb 放進這裡。
2. 打開 js/data.js，找到對應項目，把該行前面的「// 」拿掉：
      model: 'assets/models/report.glb',
3. 重新整理網頁，該節點就會用你的模型取代預設造型。

詳細的模型製作與提交方式，請看專案根目錄的 README.md 第 4 節。
